# =============================================================================
# AI-Brokerage-Infra Local Prototype — Secrets Template
# NEVER COMMIT THIS FILE WITH REAL VALUES
# Generate secrets using: ./scripts/generate-secrets.sh
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Administrative Access
# -----------------------------------------------------------------------------
POSTGRES_ADMIN_PASSWORD=admin925          # Generate: openssl rand -base64 32

# -----------------------------------------------------------------------------
# Application Database Credentials
# -----------------------------------------------------------------------------
N8N_DB_PASSWORD=admin925                  # Generate: openssl rand -base64 32
NEXTCLOUD_DB_PASSWORD=admin925            # Generate: openssl rand -base64 32
ODOO_DB_PASSWORD=admin925                 # Generate: openssl rand -base64 32

# -----------------------------------------------------------------------------
# n8n Security
# -----------------------------------------------------------------------------
N8N_ENCRYPTION_KEY=               # Generate: openssl rand -hex 16 (32 chars)
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=admin925          # Generate: openssl rand -base64 24

# -----------------------------------------------------------------------------
# Application Admin Accounts
# -----------------------------------------------------------------------------
PGADMIN_EMAIL=admin@localhost
PGADMIN_PASSWORD=admin925                 # Manual entry, 12+ characters
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=admin925         # Manual entry, 12+ characters
ODOO_ADMIN_PASSWORD=admin925              # Manual entry, 12+ characters

# -----------------------------------------------------------------------------
# Traefik Dashboard Protection
# -----------------------------------------------------------------------------
TRAEFIK_DASHBOARD_AUTH=admin925           # Generate: htpasswd -nb admin <password>